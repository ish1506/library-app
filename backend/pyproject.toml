[project]
name = "library-backend"
version = "0.1.0"
description = "Library application API"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
]

[tool.uv]
package = false
