# Docker Compose configuration. Use URL-safe PostgreSQL credentials in DATABASE_URL.
POSTGRES_DB=library_db
POSTGRES_USER=library_app
POSTGRES_PASSWORD=library_app_password
DATABASE_URL=postgresql+psycopg://library_app:library_app_password@db:5432/library_db
JWT_SECRET_KEY=replace-with-a-cryptographically-random-secret
